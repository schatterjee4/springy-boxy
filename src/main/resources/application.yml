spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    database: H2
  main:
    # TODO: banner-mode: off doesn't work?
    show-banner: false
    #banner-mode: off
